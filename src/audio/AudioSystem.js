import * as THREE from 'three';
export class AudioSystem{constructor(camera){this.listener=new THREE.AudioListener();camera.add(this.listener);this.ctx=null;this.layers=[];this.enabled=false}start(){if(this.enabled)return;this.ctx=this.listener.context;const mk=(freq,type='sine',gain=.02)=>{const o=this.ctx.createOscillator(),g=this.ctx.createGain(),f=this.ctx.createBiquadFilter();o.type=type;o.frequency.value=freq;f.type='lowpass';f.frequency.value=900;g.gain.value=gain;o.connect(f);f.connect(g);g.connect(this.ctx.destination);o.start();this.layers.push({o,g,f,base:gain})};mk(55,'sine',.018);mk(118,'triangle',.012);mk(310,'sine',.006);this.enabled=true}update(dt,pos,d){if(!this.enabled)return;const t=this.ctx.currentTime;this.layers.forEach((l,i)=>{const wet=d.includes('Aquarium')||d.includes('Water')?.04:.015;l.g.gain.setTargetAtTime(l.base+Math.sin(t*.08+i)*.004+wet*(i===2),t,.5);l.f.frequency.setTargetAtTime(d.includes('Airport')?1450:d.includes('Garden')?760:980,t,1)})}}
