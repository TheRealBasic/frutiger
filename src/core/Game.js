import * as THREE from 'three';import {Renderer} from '../rendering/Renderer.js';import {Player} from '../player/Player.js';import {World} from '../world/World.js';import {AudioSystem} from '../audio/AudioSystem.js';import {UI} from '../ui/UI.js';
export class Game{constructor(){this.clock=new THREE.Clock();this.scene=new THREE.Scene();this.scene.fog=new THREE.FogExp2(0xbfefff,.012);this.camera=new THREE.PerspectiveCamera(78,innerWidth/innerHeight,.1,900);this.renderer=new Renderer(this.scene,this.camera);this.audio=new AudioSystem(this.camera);this.player=new Player(this.camera,document.body);this.world=new World(this.scene,this.player,this.audio);this.ui=new UI(this.player,this.audio,this.world);addEventListener('resize',()=>this.resize());this.resize();this.loop()}resize(){this.camera.aspect=innerWidth/innerHeight;this.camera.updateProjectionMatrix();this.renderer.resize()}loop(){requestAnimationFrame(()=>this.loop());const dt=Math.min(.05,this.clock.getDelta());this.player.update(dt);this.world.update(dt);this.audio.update(dt,this.player.position,this.world.currentDistrictName);this.renderer.render(dt)}}
