import * as THREE from 'three';
export class Player{constructor(camera,dom){this.camera=camera;this.dom=dom;this.position=new THREE.Vector3(0,2,0);this.velocity=new THREE.Vector3();this.yaw=0;this.pitch=0;this.keys={};this.speed=7;this.sprint=12;this.fov=78;this.bob=0;dom.addEventListener('click',()=>dom.requestPointerLock());addEventListener('keydown',e=>{this.keys[e.code]=true;if(e.code==='KeyF')this.fov=this.fov===78?96:78});addEventListener('keyup',e=>this.keys[e.code]=false);addEventListener('mousemove',e=>{if(document.pointerLockElement===dom){this.yaw-=e.movementX*.0022;this.pitch=Math.max(-1.35,Math.min(1.35,this.pitch-e.movementY*.002))}})}update(dt){const dir=new THREE.Vector3((this.keys.KeyD?1:0)-(this.keys.KeyA?1:0),0,(this.keys.KeyS?1:0)-(this.keys.KeyW?1:0));if(dir.lengthSq())dir.normalize().applyAxisAngle(new THREE.Vector3(0,1,0),this.yaw);const target=dir.multiplyScalar(this.keys.ShiftLeft?this.sprint:this.speed);this.velocity.lerp(target,1-Math.pow(.001,dt));this.position.addScaledVector(this.velocity,dt);this.position.y=2+Math.sin(this.bob)*.045;this.bob+=this.velocity.length()*dt*1.7;this.camera.position.copy(this.position);this.camera.rotation.set(this.pitch+Math.sin(this.bob*.5)*.006,this.yaw,Math.sin(this.bob)*.008,'YXZ');this.camera.fov+=(this.fov+(this.keys.ShiftLeft?4:0)-this.camera.fov)*dt*5;this.camera.updateProjectionMatrix()}}
